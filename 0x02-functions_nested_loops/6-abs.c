#include "main.h"

/**
 * _abs - computes the absolute value of a number
 * @n: the number to be checked
 * Return: the abs value of n
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	if (n < 0)
		return (-n);
}
