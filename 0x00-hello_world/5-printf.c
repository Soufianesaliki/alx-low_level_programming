#include <stdio.h>

/**
 * main - printing a msg
 * m: a string containing the message
 * Return: 0
*/

int main(void)
{
	char m[80] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", m);
	return (0);
}
