#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10,
 * starting from 0
 * i: loop variable
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d");
	putchar('\n');
	return (0);
}
